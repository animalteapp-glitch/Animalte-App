name: animalte_app
description: Animalte - Next Generation Animation Studio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔐 AUTHENTICATION - YEH VERSION USE KARO
  firebase_core: ^2.24.0
  firebase_auth: ^4.18.0
  google_sign_in: ^6.1.5
  
  # 🎨 UI PACKAGES - SIMPLE RAKHO
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.4
  provider: ^6.0.5
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
