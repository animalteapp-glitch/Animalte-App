name: animalte_app
description: Animalte - Professional Drawing & Animation Studio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.3
  animated_text_kit: ^4.2.2
  
  # Compatible versions for older Dart SDK
  glassmorphism: ^2.0.0  # GlassKit alternative
  liquid_swipe: ^2.1.0
  flutter_vector_icons: ^2.0.0
  confetti: ^0.7.0
  audioplayers: ^1.1.1  # Ye version 2.18.2+ ke liye compatible hai
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/sounds/
    - assets/icons/
