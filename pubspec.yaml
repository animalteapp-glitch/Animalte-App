name: animalte_app
description: Animalte - Professional Drawing & Animation Studio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.10.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.3.0
  provider: ^6.0.5
  # audioplayers HATA DO - Zapp.run compatible nahi hai

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
